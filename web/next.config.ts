import type { NextConfig } from 'next'

const nextConfig: NextConfig = {
  // Permite que Vercel detecte el proyecto correctamente
  // desde la subcarpeta Proyecto_1/web/
}

export default nextConfig
